#include "connect_try.h"
#include "common.h"
#include <stdio.h>
#include <string.h>
#include "SEEKFREE_MT9V032.h"
//int row = 60;
//int col = 80;
//u8 image2[60][80];
//extern u8 image[ROW][COL];
//extern volatile int threshold;
//void dfs(int x, int y)
//{//深度优先搜索
//	if (image[x][y]>threshold || image2[x][y])
//	{//曾经访问过这个格子，或者当前格子是白色
//		return;
//	}
//	image2[x][y] = 1;	//标记（x, y）已访问过
// 
//	//递归访问周围的八个格子
//	dfs(x-1, y-1);	dfs(x-1, y);	dfs(x-1, y+1);
//	dfs(x, y-1);					dfs(x, y+1);
//	dfs(x+1, y-1);	dfs(x+1, y);	dfs(x+1, y+1);

//}

